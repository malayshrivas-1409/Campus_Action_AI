FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy application
COPY . .

# Build frontend (for production-like environment)
RUN npm run build

# Expose port
EXPOSE 5173

# Serve built app with a simple server or dev server
CMD ["npm", "run", "dev"]
