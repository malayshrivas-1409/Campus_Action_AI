# Campus Action AI - Postman Collection Test Report

**Generated**: 2024-01-19  
**Status**: ⚠️ **Manual Testing Required** (MCP server auth pending)  
**API Version**: 0.3.0  
**Backend URL**: `http://localhost:8000`

---

## Collection Overview

**File**: `.postman.json`  
**Name**: Campus Action AI API  
**Total Requests**: 17  
**Organized by**: 7 endpoint categories

### Collection Structure:
```
├── Health & Root (2 requests)
├── Authentication (3 requests)
├── Documents (4 requests)
├── Actions (3 requests)
├── Search (3 requests)
├── Students (3 requests)
└── Chat (3 requests)
```

---

## Endpoint Analysis & Test Coverage

### ✅ 1. Health & Root (2 requests)
| Endpoint | Method | Status | Notes |
|----------|--------|--------|-------|
| `/` | GET | ✅ Ready | Root endpoint - no auth needed |
| `/health` | GET | ✅ Ready | Health check - no auth needed |

**Expected Responses**:
```json
// GET /
{
  "message": "Welcome to Campus Action AI",
  "version": "0.3.0",
  "docs": "/docs",
  "health": "/health",
  "environment": "development"
}

// GET /health
{
  "status": "healthy",
  "message": "Campus Action AI API is running"
}
```

---

### ✅ 2. Authentication (3 requests)
| Endpoint | Method | Status | Implementation |
|----------|--------|--------|---|
| `/api/v1/auth/signup` | POST | ✅ Implemented | Full user registration |
| `/api/v1/auth/login` | POST | ✅ Implemented | JWT token generation |
| `/api/v1/auth/me` | GET | ✅ Implemented | Requires Bearer token |

**Test Sequence**:
1. **Signup** → Get `access_token`
2. **Login** → Use token for authenticated requests
3. **Get Current User** → Verify token + user data

**Potential Issues**:
- ⚠️ Email validation: Make sure test emails are valid format
- ⚠️ Password requirements: Should validate "SecurePassword123!" format
- ⚠️ Duplicate signup: Second signup with same email should return 400

**Test Data**:
```json
{
  "email": "test.student@example.com",
  "name": "Test Student",
  "password": "SecurePassword123!",
  "role": "student"
}
```

---

### ⚠️ 3. Documents (4 requests)
| Endpoint | Method | Status | Notes |
|----------|--------|--------|-------|
| `/api/v1/documents/upload` | POST | ⚠️ Form Data | Requires PDF file |
| `/api/v1/documents` | GET | ✅ Ready | List with pagination |
| `/api/v1/documents/{id}` | GET | ✅ Ready | Single document detail |
| `/api/v1/documents/{id}` | DELETE | ✅ Ready | Soft delete document |

**Issues Found**:
1. **Upload Form Data** - Collection shows form fields but missing actual file handling
2. **User Isolation** - Document endpoints properly filter by `uploaded_by` ✅
3. **Auth Headers** - All requests properly include Bearer token ✅

**Required Fixes**:
```postman
// Upload endpoint needs:
- Binary PDF file attachment (formdata key: "file")
- Form fields: "title" (string), "document_type" (enum)
- Content-Type header: auto-set by Postman for multipart/form-data
```

**File Size Validation**:
- Max upload: 50MB (from `.env`)
- Min file size: 1KB (from code)
- Allowed types: `.pdf` only

---

### ✅ 4. Actions (3 requests)
| Endpoint | Method | Status | Notes |
|----------|--------|--------|-------|
| `/api/v1/actions/extract` | POST | ✅ Implemented | Extract from document |
| `/api/v1/actions/document/{id}` | GET | ✅ Implemented | List actions for doc |
| `/api/v1/actions/{id}` | GET | ✅ Implemented | Get specific action |

**Test Flow**:
1. Upload document
2. Extract actions via LLM
3. Fetch extracted actions
4. Get individual action details

**LLM Dependency**:
- Requires `GROQ_API_KEY` configured in `.env` ✅
- Model: `openai/gpt-oss-20b` ✅
- May fail if LLM service unavailable → should return meaningful error

---

### ⚠️ 5. Search (3 requests)
| Endpoint | Method | Status | Implementation |
|----------|--------|--------|---|
| `/api/v1/search/vector` | POST | ⚠️ Vector DB Dependent | Requires pgvector setup |
| `/api/v1/search/keyword` | POST | ⚠️ Keyword Search | Uses ILIKE (SQL) |
| `/api/v1/search/info` | GET | ✅ Ready | Get embedding model info |

**Potential Issues**:
1. **Vector Search Prerequisites**:
   - PostgreSQL with pgvector extension installed
   - Document chunks must have valid embeddings (384-dimensional)
   - Query vector must be exactly 384-dimensional

2. **User Isolation**:
   - ✅ Both search methods filter by `current_user.id`
   - ✅ Cannot access other users' documents

3. **Error Scenarios**:
   - ❌ No embeddings found → Returns empty results (expected)
   - ❌ Invalid query → Should validate query length (1-500 chars)
   - ⚠️ Transaction abort if embedding dimension mismatch

**Search Endpoints Need**:
```json
{
  "query": "placement eligibility criteria",
  "limit": 10,
  "threshold": 0.5
}
```

**Embedding Info Expected**:
```json
{
  "model_name": "BAAI/bge-small-en-v1.5",
  "embedding_dimension": 384,
  "service_status": "ready"
}
```

---

### ⚠️ 6. Students (3 requests)
| Endpoint | Method | Status | Notes |
|----------|--------|--------|-------|
| `/api/v1/students/profile` | POST | ⚠️ Endpoint Name Mismatch | Collection says endpoint but code shows different route |
| `/api/v1/students/me` | GET | ✅ Implemented | Get current student profile |
| `/api/v1/students/me` | PUT | ✅ Implemented | Update current student profile |

**Issues**:
1. **Collection Inconsistency** - Creation endpoint uses `/students` but code implements `/students/profile`
2. **Profile Existence Check** - Should verify student profile exists before operations
3. **Data Validation**:
   - CGPA: 0-10 scale (should validate)
   - Backlogs: Non-negative integer
   - Department: Must match valid departments
   - Batch: Should be 4-digit year

**Test Data**:
```json
{
  "roll_number": "2024001",
  "department": "CSE",
  "batch": 2024,
  "cgpa": 8.5,
  "backlogs": 0
}
```

---

### ⚠️ 7. Chat (3 requests)
| Endpoint | Method | Status | Issues |
|----------|--------|--------|--------|
| `/api/v1/chat/conversations` | POST | ⚠️ Missing | Not in collection |
| `/api/v1/chat/message` | POST | ✅ Implemented | Requires RAG + LLM |
| `/api/v1/chat/conversations/{id}` | GET | ✅ Implemented | Fetch conversation history |

**Chat Requires**:
1. **RAG Service** - Must have documents uploaded + embeddings generated
2. **LLM Service** - Groq API must be configured
3. **Database** - Conversation + Message storage

**Potential Issues**:
- ⚠️ Chat will fail if no documents uploaded (LLM will still respond, but won't have context)
- ⚠️ Long response times if document search takes time
- ✅ Proper transaction handling implemented to avoid session corruption

**Test Conversation Request**:
```json
{
  "conversation_id": "existing-id-or-null",
  "message": "What are the eligibility criteria for placement?",
  "top_k": 10,
  "selected_document_ids": ["doc-id-1"]
}
```

---

## Environment Variables Required

The collection uses these variables (currently empty):

```javascript
{
  "base_url": "http://localhost:8000",  // ✅ Configured
  "access_token": "",                   // ← Fill after login
  "document_id": "",                    // ← Fill after upload
  "action_id": "",                      // ← Fill after extract
  "student_id": "",                     // ← Fill after profile creation
  "user_id": "",                        // ← Fill after signup
  "conversation_id": ""                 // ← Fill after chat start
}
```

**Setup Instructions**:
1. Run `GET /health` → Verify API is running
2. Run `POST /auth/signup` → Get `user_id` + `access_token`
3. Run subsequent requests using the token

---

## Database & External Dependencies

| Dependency | Status | Required For | Notes |
|------------|--------|--------------|-------|
| PostgreSQL | ⚠️ Must be running | All data operations | With pgvector extension |
| Redis | Optional | Caching/Rate limiting | Not required for basic tests |
| Groq API | ⚠️ Configured | LLM (chat, actions) | Set `GROQ_API_KEY` in `.env` ✅ |
| Embeddings Model | ⚠️ First download | Vector search | BAAI/bge-small-en-v1.5 auto-downloads |

---

## Issues & Fixes Needed

### 🔴 Critical
1. **Endpoint Route Mismatch** (Students)
   - Collection: `POST /api/v1/students`
   - Code: `POST /api/v1/students/profile`
   - **Fix**: Update collection to `/students/profile` or change code to `/students`

2. **Missing Endpoints in Collection**
   - `GET /api/v1/chat/conversations` - List all conversations
   - `DELETE /api/v1/chat/conversations/{id}` - Delete conversation
   - `PATCH /api/v1/notifications/{id}` - Mark notification as read
   - **Fix**: Add missing requests to collection

### ⚠️ Important
1. **Missing Endpoints in Backend**
   - `GET /api/v1/rag/*` - RAG endpoints not in collection
   - `GET /api/v1/embeddings/*` - Embedding endpoints not in collection
   - `GET /api/v1/notifications` - Notifications API
   - **Fix**: Check `/backend/app/api/` for other routers to add

2. **Form Data File Upload**
   - Collection upload request needs actual PDF binary
   - **Fix**: Add test PDF file reference in Postman environment

3. **No Test Scripts**
   - Collection has no `Tests` tab validations
   - **Fix**: Add assertions like:
     ```javascript
     pm.test("Status code is 200", () => {
       pm.response.to.have.status(200);
     });
     pm.test("Response has access_token", () => {
       pm.expect(pm.response.json()).to.have.property('access_token');
     });
     ```

### 📋 Minor
1. **No Request Descriptions** - Add purpose/notes to each request
2. **No Error Cases** - Missing 4xx/5xx test variations
3. **No Pagination Tests** - Could test skip/limit on document list
4. **No Timeout Handling** - Could add timeout for long-running ops

---

## Recommended Test Execution Order

```
1. Health Checks (no auth)
   ✓ GET /health
   ✓ GET /

2. Authentication Flow
   ✓ POST /auth/signup → extract access_token, user_id
   ✓ POST /auth/login → verify token works
   ✓ GET /auth/me → confirm user data

3. Student Setup
   ✓ POST /students/profile → create profile
   ✓ GET /students/me → verify profile exists

4. Document Management
   ✓ POST /documents/upload → upload test PDF, extract document_id
   ✓ GET /documents → list with pagination
   ✓ GET /documents/{id} → fetch single document

5. Action Extraction
   ✓ POST /actions/extract → extract actions from document
   ✓ GET /actions/document/{id} → list extracted actions
   ✓ GET /actions/{id} → get single action

6. Search
   ✓ GET /search/info → verify embedding model
   ✓ POST /search/vector → semantic search
   ✓ POST /search/keyword → keyword search

7. Chat
   ✓ POST /chat/message → send question with document context
   ✓ GET /chat/conversations/{id} → fetch conversation history

8. Cleanup
   ✓ DELETE /documents/{id} → delete test document
```

---

## How to Run Tests in Postman

### Option 1: Postman GUI
```
1. Import .postman.json into Postman
2. Create environment with base_url = http://localhost:8000
3. Run "Collection Runner" (top-left button)
4. Select collection + environment
5. Click "Run"
```

### Option 2: Postman CLI (Newman)
```bash
npm install -g newman
newman run .postman.json \
  -e postman-env.json \
  --bail \
  --reporters cli,json
```

### Option 3: GitHub Actions (CI/CD)
```yaml
- name: Run API tests
  run: |
    newman run .postman.json \
      -e postman-env.json \
      --reporters json \
      --reporter-json-export test-results.json
```

---

## Summary

| Category | Status | Count | Notes |
|----------|--------|-------|-------|
| ✅ Ready | 8 | Good to test immediately |
| ⚠️ Needs Setup | 6 | Require DB/LLM/files |
| ❌ Issues | 3 | Endpoint mismatches |
| 📋 Missing | 5+ | Not in collection |
| **Total** | **~17** | Partially complete |

### Next Steps:
1. ✅ Ensure PostgreSQL + Redis running
2. ✅ Verify `GROQ_API_KEY` set in `.env`
3. ⚠️ Fix student profile endpoint mismatch
4. ⚠️ Add missing endpoints to collection
5. 🧪 Execute full test suite in order
6. 📊 Document any failures with error logs

---

**Test Coverage**: 17 / ~22+ endpoints = ~77% collection coverage

